name: flutter_read_device
description: "A production-ready Flutter service layer that merges device_info_plus and package_info_plus into a single clean SDK-style API."
version: 1.0.0
homepage: https://github.com/techaak/flutter_read_device.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
